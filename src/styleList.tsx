import { Text, View, StyleSheet } from 'react-native';

export const primayColor = '#604a9c';

export const styleList = StyleSheet.create({
    container: {
        backgroundColor: '#a5bfcf',
        height: '100%',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
    }
});